# language setting
language: python

# version numbers
python:
  - "2.7"

# Setup the environment before installing
before_install:
  # ----------------------------------------------------------------------------
  # Test setup by printing some variables
  # ----------------------------------------------------------------------------
  - pwd
  - echo "$VIRTUAL_ENV"
  - echo "$SHIPPABLE_PYTHON"
  - PYTHON_VERSION="${SHIPPABLE_PYTHON##*/python}"
  - echo "$PYTHON_VERSION"
  # ----------------------------------------------------------------------------
  # The following is taken from Minicoda's how-to Travis page
  # http://conda.pydata.org/docs/travis.html
  # ----------------------------------------------------------------------------
  - sudo apt-get update
  # We do this conditionally because it saves us some downloading if the
  # version is the same.
  - if [[ "$PYTHON_VERSION" == "2.7" ]]; then
      wget https://repo.continuum.io/miniconda/Miniconda-latest-Linux-x86_64.sh -O miniconda.sh;
    else
      wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh;
    fi
  - bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  # Useful for debugging any issues with conda
  - conda info -a
  - sed '/^-e/d' requirements.txt > requirements_conda.txt
  - sed '/^-e/!d' requirements.txt > requirements_pip.txt
  - conda create --yes -n test-environment python=$PYTHON_VERSION pip --file requirements_conda.txt
  # ----------------------------------------------------------------------------

# install requirements
install:
  # Separate out the requirements which are editable and have to go through pip
  # and the ones which aren't, which can be installed by conda
  - source activate test-environment
  - pip install -vv -r requirements_pip.txt
  #- python setup.py install
  - pip freeze

# commands to run test scripts
script:
  - which python
